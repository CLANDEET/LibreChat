/**
 * BKL Prism 브랜드 표기.
 *
 * 로그인 히어로와 채팅 하단 푸터 두 곳에 같은 태그라인이 나가므로 한곳에 둔다 —
 * 각자 리터럴을 들고 있으면 한쪽만 고쳐져 표기가 갈라진다.
 */
export const BKL_TAGLINE = '흩어진 지식을 하나의 스펙트럼으로';

/**
 * 브라우저 탭 · 화면 제목에 쓰는 서비스명.
 *
 * LibreChat 원본은 이 값을 서버의 `APP_TITLE` 환경변수에서 받아
 * `startupConfig.appTitle` 로 내려주고, 클라이언트는 그걸 localStorage 에
 * 캐시해서 문서검색·프로젝트 화면 제목까지 파생시킨다. 그래서 배포 스택의
 * env 하나가 옛 이름으로 남아 있으면 이미지를 새로 올려도 탭 제목이 바뀌지
 * 않고, 캐시까지 물려 여러 화면이 함께 옛 이름을 달고 있게 된다.
 *
 * 서비스명은 배포 환경별로 달라질 값이 아니라 브랜드 상수이므로 코드에서
 * 확정한다. `APP_TITLE` 은 이메일 발신자명·2FA issuer 처럼 서버에서만 쓰이는
 * 용도로 남는다.
 */
export const BKL_APP_TITLE = 'BKL Prism';
